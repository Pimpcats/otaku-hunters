# ui/

UI art (transparent PNG). Subfolders: `icons/` (passive icons), `frames/` (panels/
cards), `bars/` (HP/XP), `title/` (logo + text treatments).

**Status:** the HUD/level-up UI is currently drawn procedurally and does NOT yet
reference these files. These are drop zones; wiring each into the HUD is a follow-up
(it touches UI code). Drop art in + list it in `ASSET_INDEX.md`; ping me to wire.
