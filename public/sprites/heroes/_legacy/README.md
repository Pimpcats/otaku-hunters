# heroes/_legacy/

Superseded placeholder character art, still used until the new per-character sheets
(`kohai/`, `sensei/`, `ronin/`) are generated and wired:
- `roster_sheet.webp` — 8×4 grid, 192×256 cells (blonde = Kōhai, brown = Sensei).
- `goth_girl.webp` — 6×3 grid, 256×341 cells (Rōnin).

Wired in `src/ui/playerSheet.ts`. Delete once real sheets replace them.
